---
phase: 05-onboarding-first-impressions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/empty-state.tsx
  - src/app/(dashboard)/contacts/page.tsx
  - src/app/(dashboard)/drafts/page.tsx
  - src/components/drafts/draft-list.tsx
  - src/components/dashboard/risk-contacts.tsx
  - src/components/dashboard/triage-contacts.tsx
  - src/components/dashboard/pending-actions.tsx
  - src/components/dashboard/outreach-analytics.tsx
  - src/components/contacts/contact-detail.tsx
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements:
  - ONBD-04
  - ONBD-06

must_haves:
  truths:
    - "Every empty list across the app shows a unified empty state with icon, heading, description, and CTA button"
    - "Contacts page empty state says 'Process Your First Meetings' with a CTA"
    - "Drafts page empty state says 'Set Up Outreach Schedule' with a CTA"
    - "Dashboard widgets show consistent empty states (not just CheckCircle + text)"
    - "Contact detail page loading state uses Skeleton components instead of raw animate-pulse divs"
    - "All async pages show shape-matched skeleton placeholders"
  artifacts:
    - path: "src/components/ui/empty-state.tsx"
      provides: "Unified EmptyState component"
      exports: ["EmptyState"]
      contains: "EmptyState"
    - path: "src/components/contacts/contact-detail.tsx"
      provides: "Fixed loading state with Skeleton instead of animate-pulse"
      contains: "Skeleton"
  key_links:
    - from: "src/app/(dashboard)/contacts/page.tsx"
      to: "src/components/ui/empty-state.tsx"
      via: "EmptyState import"
      pattern: "EmptyState"
    - from: "src/components/drafts/draft-list.tsx"
      to: "src/components/ui/empty-state.tsx"
      via: "EmptyState import"
      pattern: "EmptyState"
    - from: "src/components/dashboard/risk-contacts.tsx"
      to: "src/components/ui/empty-state.tsx"
      via: "EmptyState import"
      pattern: "EmptyState"
---

<objective>
Create a unified EmptyState component and replace all inconsistent empty state patterns across the app. Fix contact detail page to use shaped Skeleton loading instead of raw animate-pulse divs.

Purpose: ONBD-04 + ONBD-06 -- standardize empty and loading experiences so every page feels polished and guides users toward productive actions.
Output: Reusable EmptyState component, all empty states unified, contact detail loading fixed.
</objective>

<execution_context>
@/Users/Matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-onboarding-first-impressions/05-CONTEXT.md

<interfaces>
<!-- Current inconsistent empty state patterns to replace -->

From src/app/(dashboard)/contacts/page.tsx (lines 156-164):
- Uses Loader2 icon (spinning loader for empty state -- misleading)
- "No contacts found" heading
- Conditional text based on search/filter state

From src/components/drafts/draft-list.tsx (lines 26-35):
- Uses Mail icon
- "No drafts to review" heading
- Static description text, no CTA button

From src/components/dashboard/risk-contacts.tsx (lines 33-39):
- Uses CheckCircle icon
- "No contacts at risk" -- single text line, no heading/description structure

From src/components/dashboard/triage-contacts.tsx:
- Same CheckCircle pattern as risk-contacts

From src/components/dashboard/pending-actions.tsx:
- Same CheckCircle pattern as risk-contacts

From src/components/dashboard/outreach-analytics.tsx (lines 101-104):
- Uses BarChart3 icon
- Has description but no CTA

From src/components/contacts/contact-detail.tsx (lines 179-180):
- Loading state uses raw "animate-pulse" divs with bg-muted -- NOT Skeleton component
- Must be replaced with Skeleton component for shape-matched loading

From src/app/(dashboard)/dashboard/page.tsx (lines 86-97):
- Loading uses Skeleton className="h-24 rounded-xl" (generic rectangles)
- Should use shape-matched skeletons that look like stat cards and widget cards
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified EmptyState component and replace all page empty states</name>
  <files>src/components/ui/empty-state.tsx, src/app/(dashboard)/contacts/page.tsx, src/components/drafts/draft-list.tsx</files>
  <read_first>src/app/(dashboard)/contacts/page.tsx, src/components/drafts/draft-list.tsx</read_first>
  <action>
1. Create src/components/ui/empty-state.tsx:
   - "use client" not needed (pure presentational, no hooks)
   - Props interface EmptyStateProps:
     - icon: LucideIcon (from lucide-react)
     - heading: string
     - description: string
     - action?: { label: string; href: string } (optional CTA button)
     - compact?: boolean (for dashboard widget empty states vs full page)
   - Renders centered flex column:
     - When compact=false (default): icon h-12 w-12 text-muted-foreground/50 mb-4, heading as h3 text-lg font-medium, description as p text-sm text-muted-foreground mt-1, if action: Button variant="default" as Link to action.href with action.label, mt-4
     - When compact=true: icon h-8 w-8, heading as p text-sm font-medium, description as p text-xs, smaller padding py-6
   - Wrap in div className="flex flex-col items-center justify-center py-12 text-center" (py-6 when compact)
   - Export EmptyState and EmptyStateProps

2. Update src/app/(dashboard)/contacts/page.tsx empty state (lines 155-164):
   - Import EmptyState from "@/components/ui/empty-state"
   - Import Users from lucide-react (already imported)
   - Replace the inline empty state div with:
     - If search or categoryFilter active: EmptyState with Search icon, heading="No contacts found", description="Try adjusting your search or filter criteria." (no CTA)
     - If no filters: EmptyState with Users icon, heading="No contacts yet", description="Process your first meetings to start building your network.", action={ label: "Process Meetings", href: "/settings/integrations" }

3. Update src/components/drafts/draft-list.tsx empty state (lines 26-35):
   - Import EmptyState from "@/components/ui/empty-state"
   - Replace inline empty state with: EmptyState with Mail icon (already imported), heading="No drafts to review", description="AI-generated outreach drafts will appear here for your review.", action={ label: "Set Up Outreach Schedule", href: "/settings/schedule" }
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && grep -q "export.*EmptyState" src/components/ui/empty-state.tsx && grep -q "EmptyState" src/app/\(dashboard\)/contacts/page.tsx && grep -q "EmptyState" src/components/drafts/draft-list.tsx && echo "PASS"</automated>
  </verify>
  <done>
  - EmptyState component exists with icon, heading, description, optional CTA, compact mode
  - Contacts page uses EmptyState with contextual messaging (filtered vs unfiltered)
  - Drafts list uses EmptyState with "Set Up Outreach Schedule" CTA
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace dashboard widget empty states and fix all skeleton loading states</name>
  <files>src/components/dashboard/risk-contacts.tsx, src/components/dashboard/triage-contacts.tsx, src/components/dashboard/pending-actions.tsx, src/components/dashboard/outreach-analytics.tsx, src/components/contacts/contact-detail.tsx, src/app/(dashboard)/dashboard/page.tsx</files>
  <read_first>src/components/dashboard/risk-contacts.tsx, src/components/dashboard/triage-contacts.tsx, src/components/dashboard/pending-actions.tsx, src/components/contacts/contact-detail.tsx, src/app/(dashboard)/dashboard/page.tsx</read_first>
  <action>
1. Update dashboard widget empty states to use EmptyState compact mode:
   - risk-contacts.tsx: Replace CheckCircle inline empty state with EmptyState compact=true, icon=CheckCircle (keep same icon -- it communicates "all good"), heading="No contacts at risk", description="All contacts are on track"
   - triage-contacts.tsx: Same pattern -- EmptyState compact=true, icon=CheckCircle, heading="No triage needed", description="All contacts have been reviewed"
   - pending-actions.tsx: EmptyState compact=true, icon=CheckCircle, heading="No pending actions", description="All action items are complete"
   - outreach-analytics.tsx: EmptyState compact=true (NOT full height), icon=BarChart3 (keep existing), heading="No outreach data yet", description="Drafts will appear here once generated"

2. Fix contact detail loading state (src/components/contacts/contact-detail.tsx):
   - Import Skeleton from "@/components/ui/skeleton"
   - Find the loading return block (around line 175-182) that uses raw animate-pulse divs
   - Replace with shape-matched Skeleton components:
     ```
     <div className="space-y-6">
       <div className="flex items-center gap-2">
         <Skeleton className="h-8 w-8 rounded" />
         <Skeleton className="h-8 w-48" />
       </div>
       <div className="grid grid-cols-1 gap-6 lg:grid-cols-3">
         <div className="lg:col-span-2 space-y-4">
           <Skeleton className="h-64 rounded-xl" />
           <Skeleton className="h-48 rounded-xl" />
         </div>
         <div className="space-y-4">
           <Skeleton className="h-48 rounded-xl" />
           <Skeleton className="h-32 rounded-xl" />
         </div>
       </div>
     </div>
     ```

3. Improve dashboard page skeleton loading (src/app/(dashboard)/dashboard/page.tsx):
   - Update the stat card skeletons (line 87-89) to be more shape-matched:
     - Each stat card skeleton: Skeleton className="h-[106px] rounded-xl" (matches StatCard actual height better)
   - Update widget skeletons (line 91-93) to match the actual 3-column widget layout:
     - Keep 3 skeletons but use h-[300px] to better match actual widget card heights
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && grep -q "EmptyState" src/components/dashboard/risk-contacts.tsx && grep -q "EmptyState" src/components/dashboard/triage-contacts.tsx && grep -q "EmptyState" src/components/dashboard/pending-actions.tsx && grep -q "Skeleton" src/components/contacts/contact-detail.tsx && ! grep -q "animate-pulse" src/components/contacts/contact-detail.tsx && echo "PASS"</automated>
  </verify>
  <done>
  - All 4 dashboard widget empty states use EmptyState compact mode
  - Contact detail loading uses Skeleton components (no animate-pulse divs)
  - Dashboard page skeletons are shape-matched to actual stat cards and widgets
  - No raw animate-pulse divs remain in any loading state
  </done>
</task>

</tasks>

<verification>
- grep -r "animate-pulse" src/components/contacts/contact-detail.tsx returns no results
- grep -r "EmptyState" src/ shows all pages using the unified component
- All empty states have icon + heading + description structure
- Contacts and drafts empty states include CTA buttons with href
</verification>

<success_criteria>
- Unified EmptyState component with icon, heading, description, optional CTA, and compact mode
- All 6 empty state locations (contacts, drafts, risk, triage, pending-actions, analytics) use EmptyState
- Contact detail loading uses Skeleton (not animate-pulse)
- Dashboard page skeletons are shape-matched
- Contacts empty state CTA points to settings/integrations, drafts to settings/schedule
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding-first-impressions/05-02-SUMMARY.md`
</output>

---
phase: 05-onboarding-first-impressions
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/components/onboarding/onboarding-wizard.tsx
  - src/components/onboarding/setup-checklist.tsx
  - src/components/onboarding/wizard-steps.tsx
  - src/lib/onboarding.ts
  - src/app/api/onboarding/status/route.ts
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/components/settings/granola-token-form.tsx
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03

must_haves:
  truths:
    - "A brand-new user who logs in for the first time sees a multi-step onboarding wizard modal"
    - "The wizard detects missing Granola connection, empty contacts, and incomplete profile"
    - "User can skip the wizard with 'I'll do this later' and see a sidebar checklist reminding them"
    - "Setup checklist in sidebar tracks completion and disappears when all steps are done"
    - "Granola connection instructions use a collapsible accordion format with retry on error"
    - "Onboarding is complete when first contact is created"
  artifacts:
    - path: "src/components/onboarding/onboarding-wizard.tsx"
      provides: "Multi-step modal wizard component"
      exports: ["OnboardingWizard"]
      contains: "Dialog"
    - path: "src/components/onboarding/setup-checklist.tsx"
      provides: "Sidebar checklist tracking onboarding progress"
      exports: ["SetupChecklist"]
      contains: "CheckCircle"
    - path: "src/components/onboarding/wizard-steps.tsx"
      provides: "Individual wizard step content components"
      exports: ["WelcomeStep", "GranolaStep", "ProfileStep", "ProcessStep"]
    - path: "src/lib/onboarding.ts"
      provides: "Onboarding status detection types and helpers"
      exports: ["OnboardingStatus"]
    - path: "src/app/api/onboarding/status/route.ts"
      provides: "API endpoint returning onboarding completion status"
      exports: ["GET"]
    - path: "src/components/settings/granola-token-form.tsx"
      provides: "Enhanced Granola form with collapsible accordion instructions"
      contains: "Collapsible"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/components/onboarding/onboarding-wizard.tsx"
      via: "OnboardingWizard rendered conditionally"
      pattern: "OnboardingWizard"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/onboarding/setup-checklist.tsx"
      via: "SetupChecklist in SidebarContent"
      pattern: "SetupChecklist"
    - from: "src/components/onboarding/onboarding-wizard.tsx"
      to: "src/app/api/onboarding/status/route.ts"
      via: "fetch /api/onboarding/status"
      pattern: "api/onboarding/status"
    - from: "src/components/onboarding/wizard-steps.tsx"
      to: "src/components/settings/granola-token-form.tsx"
      via: "GranolaStep embeds Granola connection form"
      pattern: "GranolaTokenForm"
---

<objective>
Build the onboarding wizard (multi-step modal), persistent sidebar setup checklist, and improved Granola connection instructions.

Purpose: ONBD-01 + ONBD-02 + ONBD-03 -- guide new users through setup so they don't face a confusing empty app. The wizard detects what's missing and walks them through it; the checklist persists until everything is done.
Output: OnboardingWizard modal, SetupChecklist sidebar component, enhanced Granola instructions, onboarding status API.
</objective>

<execution_context>
@/Users/Matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-onboarding-first-impressions/05-CONTEXT.md
@.planning/phases/05-onboarding-first-impressions/05-01-SUMMARY.md
@.planning/phases/05-onboarding-first-impressions/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/app/(dashboard)/layout.tsx (after Plan 01):
- SidebarFooter now contains ThemeToggle
- SidebarContent has Navigation group
- Need to add SetupChecklist as a new SidebarGroup after Navigation
- Layout is a server component; SetupChecklist must be "use client"

From src/app/(dashboard)/dashboard/page.tsx:
- Client component with useEffect + useState pattern
- Fetches /api/dashboard/stats on mount
- OnboardingWizard should render here, controlled by onboarding status

From src/components/settings/granola-token-form.tsx:
- GranolaTokenForm is a self-contained "use client" component
- Has handleSave for token submission
- Instructions currently inline as ordered list (lines 120-135)
- Loading state, connected/not-connected states, editing toggle
- Badge for connection status

From existing API patterns:
- API routes: createServerClient from @/lib/supabase/server, getUser() for auth
- Return JSON { data } or { error }
- user_settings table has: granola_refresh_token, granola_client_id columns
- contacts table has: user_id, name, etc.
- user_settings has: company, role (profile fields)

From src/components/ui/dialog.tsx:
- Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogClose exported

Shadcn components available (no accordion yet):
- dialog, button, card, badge, skeleton, input, label, separator, tabs
- Will need to use a custom collapsible pattern (ChevronDown + state toggle) since Accordion is not installed
- OR: install shadcn collapsible component via npx shadcn@latest add collapsible
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding status API and types, wizard steps, and enhanced Granola instructions</name>
  <files>src/lib/onboarding.ts, src/app/api/onboarding/status/route.ts, src/components/onboarding/wizard-steps.tsx, src/components/settings/granola-token-form.tsx</files>
  <read_first>src/components/settings/granola-token-form.tsx, src/app/api/granola/token/route.ts, src/lib/supabase/server.ts</read_first>
  <action>
1. Install shadcn collapsible component for accordion-style Granola instructions:
   - Run: npx shadcn@latest add collapsible

2. Create src/lib/onboarding.ts:
   - Export interface OnboardingStatus:
     - has_granola: boolean (Granola token connected)
     - has_contacts: boolean (at least 1 contact exists)
     - has_profile: boolean (company or role filled in user_settings)
     - is_complete: boolean (all three true)
     - completed_steps: number (count of true values)
     - total_steps: number (always 3)
   - Export type OnboardingStep = "granola" | "profile" | "contacts"
   - Export const ONBOARDING_STEPS array with { id: OnboardingStep, label: string, description: string } for each step:
     - { id: "granola", label: "Connect Granola", description: "Link your Granola account to import meetings" }
     - { id: "profile", label: "Complete Profile", description: "Add your company and role for better outreach" }
     - { id: "contacts", label: "Process Meetings", description: "Import contacts from your first meetings" }

3. Create src/app/api/onboarding/status/route.ts:
   - GET endpoint, authenticated via createServerClient + getUser()
   - Query user_settings for current user: select granola_refresh_token, company, role
   - Query contacts count: select count from contacts where user_id
   - Build OnboardingStatus response:
     - has_granola = !!settings.granola_refresh_token
     - has_profile = !!(settings.company || settings.role)
     - has_contacts = contactCount > 0
     - is_complete = has_granola && has_profile && has_contacts
     - completed_steps = count of true values
     - total_steps = 3
   - Return { data: onboardingStatus }

4. Create src/components/onboarding/wizard-steps.tsx:
   - "use client" component file with 4 exported step components
   - Each step receives: onNext: () => void, onSkip: () => void

   WelcomeStep: Simple welcome message with app logo/name, brief description of what ELITE does, "Get Started" button calling onNext, "I'll set up later" link calling onSkip

   GranolaStep: Embed a simplified version of the Granola connection flow:
     - Show brief explanation: "ELITE imports your meeting notes from Granola to build relationship intelligence."
     - Use Collapsible from shadcn for the detailed instructions:
       - CollapsibleTrigger shows "How to find your Granola credentials" with ChevronDown icon
       - CollapsibleContent shows the numbered steps (same content as granola-token-form.tsx instructions)
     - Include the refresh_token and client_id input fields with labels
     - "Connect & Continue" button that POSTs to /api/granola/token, on success calls onNext
     - On error: show inline error message (red text below button) with "Retry" capability (just keep the form visible)
     - "Skip for now" link calling onSkip

   ProfileStep: Simple form for company and role fields:
     - Two Input fields: company name, your role
     - "Save & Continue" button that PUTs to /api/settings/profile, on success calls onNext
     - "Skip for now" link calling onSkip

   ProcessStep: Final step explaining how meeting processing works:
     - Brief explanation that meetings will be processed automatically on schedule
     - "Process Now" button that POSTs to /api/meetings/process, on success calls onNext
     - "I'll do this later" link calling onSkip
     - Show success state after processing is triggered: "Processing started! Your contacts will appear shortly."

5. Update src/components/settings/granola-token-form.tsx (ONBD-03):
   - Import Collapsible, CollapsibleContent, CollapsibleTrigger from @/components/ui/collapsible
   - Import ChevronDown from lucide-react
   - Replace the static instruction list (lines 120-135) with a Collapsible:
     - CollapsibleTrigger: Button variant="ghost" with "How to get your Granola credentials" text + ChevronDown icon that rotates when open (use state + cn for transform)
     - CollapsibleContent: The existing ordered list instructions
   - Add inline error state: if save fails, show error message below the form in a red text paragraph instead of only using toast (keep toast too for consistency)
   - Add a Retry button next to the error message that re-attempts handleSave
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && test -f src/lib/onboarding.ts && test -f src/app/api/onboarding/status/route.ts && test -f src/components/onboarding/wizard-steps.tsx && grep -q "Collapsible" src/components/settings/granola-token-form.tsx && grep -q "OnboardingStatus" src/lib/onboarding.ts && echo "PASS"</automated>
  </verify>
  <done>
  - OnboardingStatus type and ONBOARDING_STEPS constant exported from lib/onboarding.ts
  - GET /api/onboarding/status returns has_granola, has_contacts, has_profile, is_complete
  - WelcomeStep, GranolaStep, ProfileStep, ProcessStep exported from wizard-steps.tsx
  - GranolaStep has collapsible instructions with inline error + retry
  - granola-token-form.tsx instructions use Collapsible accordion format with inline error + retry
  </done>
</task>

<task type="auto">
  <name>Task 2: Build OnboardingWizard modal and SetupChecklist sidebar component, wire into layout</name>
  <files>src/components/onboarding/onboarding-wizard.tsx, src/components/onboarding/setup-checklist.tsx, src/app/(dashboard)/dashboard/page.tsx, src/app/(dashboard)/layout.tsx</files>
  <read_first>src/app/(dashboard)/dashboard/page.tsx, src/app/(dashboard)/layout.tsx, src/lib/onboarding.ts, src/components/onboarding/wizard-steps.tsx</read_first>
  <action>
1. Create src/components/onboarding/onboarding-wizard.tsx:
   - "use client" component
   - Props: onboardingStatus: OnboardingStatus, onComplete: () => void
   - Internal state: currentStep (0-3 index), open (dialog open state, defaults true)
   - Steps array: ["welcome", "granola", "profile", "process"]
   - Conditionally skip steps that are already complete (e.g., if has_granola is true, skip granola step when advancing)
   - Render Dialog with DialogContent className="sm:max-w-lg":
     - Show step indicator: "Step {current} of {total}" in muted text at top
     - Render the appropriate step component based on currentStep index
     - onNext callback: advance to next incomplete step, if all done call onComplete and close dialog
     - onSkip callback: close dialog, call onComplete (user chose to skip)
   - Dialog onOpenChange: if closed, call onComplete (treat closing as skip)
   - Do NOT use DialogClose for the X button behavior -- let Dialog's built-in close handle it

2. Create src/components/onboarding/setup-checklist.tsx:
   - "use client" component
   - Fetches /api/onboarding/status on mount
   - If status.is_complete, render nothing (return null)
   - If loading, render a small Skeleton placeholder in sidebar dimensions
   - Otherwise render a SidebarGroup with SidebarGroupLabel "Setup Progress":
     - Show progress: "{completed_steps} of {total_steps} complete" as SidebarGroupLabel suffix
     - List each step from ONBOARDING_STEPS:
       - If step is complete: CheckCircle2 icon (green), text with line-through styling
       - If step is incomplete: Circle icon (muted), text as link to relevant settings page:
         - granola -> /settings/integrations
         - profile -> /settings/profile
         - contacts -> /contacts (with note to process meetings)
     - Use SidebarMenu, SidebarMenuItem, SidebarMenuButton for consistent sidebar styling
     - Each incomplete step is clickable (navigates to the relevant page)

3. Update src/app/(dashboard)/layout.tsx:
   - Import SetupChecklist from "@/components/onboarding/setup-checklist"
   - Add SetupChecklist as a new SidebarGroup AFTER the Navigation SidebarGroup in SidebarContent
   - Place it between SidebarContent and SidebarFooter (inside SidebarContent, after the nav group)
   - The layout remains a server component; SetupChecklist handles its own client boundary

4. Update src/app/(dashboard)/dashboard/page.tsx:
   - Import OnboardingWizard from "@/components/onboarding/onboarding-wizard"
   - Import OnboardingStatus type from "@/lib/onboarding"
   - Add state: onboardingStatus (OnboardingStatus | null), showWizard (boolean)
   - In fetchStats (or a separate useEffect), also fetch /api/onboarding/status
   - If status.is_complete is false AND localStorage key "elite_onboarding_dismissed" is NOT set:
     - Set showWizard = true
   - Render OnboardingWizard conditionally:
     - {showWizard && onboardingStatus && <OnboardingWizard onboardingStatus={onboardingStatus} onComplete={handleWizardComplete} />}
   - handleWizardComplete: set showWizard=false, set localStorage "elite_onboarding_dismissed" = "true"
   - This ensures wizard shows ONCE per browser. User can still use sidebar checklist for remaining steps.
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && grep -q "OnboardingWizard" src/app/\(dashboard\)/dashboard/page.tsx && grep -q "SetupChecklist" src/app/\(dashboard\)/layout.tsx && grep -q "Dialog" src/components/onboarding/onboarding-wizard.tsx && grep -q "ONBOARDING_STEPS" src/components/onboarding/setup-checklist.tsx && echo "PASS"</automated>
  </verify>
  <done>
  - OnboardingWizard renders as Dialog on dashboard for new users (not dismissed)
  - Wizard walks through Welcome -> Granola -> Profile -> Process, skipping completed steps
  - User can skip/close wizard at any time; localStorage prevents re-showing
  - SetupChecklist appears in sidebar with step-by-step progress tracking
  - Checklist links incomplete steps to relevant settings pages
  - Checklist disappears when all steps are complete (is_complete = true)
  </done>
</task>

</tasks>

<verification>
- New user flow: /api/onboarding/status returns is_complete=false -> wizard appears -> user can navigate steps or skip
- Sidebar shows checklist with progress indicators until all steps done
- Granola instructions in settings use collapsible accordion with inline error + retry
- localStorage "elite_onboarding_dismissed" prevents wizard re-appearance after skip
- Completed steps show green checkmark in sidebar checklist
</verification>

<success_criteria>
- First-time user sees wizard modal on dashboard page
- Wizard detects missing Granola, contacts, and profile
- Each wizard step has skip option ("I'll do this later")
- Sidebar checklist tracks progress and links to relevant pages
- Checklist disappears when onboarding is complete
- Granola instructions use collapsible format in both wizard and settings page
- Inline error with retry on Granola connection failure
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding-first-impressions/05-03-SUMMARY.md`
</output>

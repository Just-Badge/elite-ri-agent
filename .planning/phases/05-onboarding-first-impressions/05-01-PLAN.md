---
phase: 05-onboarding-first-impressions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/(dashboard)/layout.tsx
  - src/components/theme-toggle.tsx
autonomous: true
requirements:
  - ONBD-05

must_haves:
  truths:
    - "App defaults to system color scheme preference on first visit"
    - "User can toggle between light and dark mode from sidebar footer"
    - "Theme choice persists across page refreshes and sessions"
    - "All existing UI elements render correctly in both light and dark mode"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "ThemeProvider wrapping entire app"
      contains: "ThemeProvider"
    - path: "src/components/theme-toggle.tsx"
      provides: "Theme toggle button component"
      contains: "useTheme"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Theme toggle placed in SidebarFooter"
      contains: "SidebarFooter"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next-themes"
      via: "ThemeProvider wrapper"
      pattern: "ThemeProvider"
    - from: "src/components/theme-toggle.tsx"
      to: "next-themes"
      via: "useTheme hook"
      pattern: "useTheme"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/theme-toggle.tsx"
      via: "SidebarFooter import"
      pattern: "ThemeToggle"
---

<objective>
Wire up dark mode across the app using next-themes (already installed v0.4.6) and add a toggle in the sidebar footer.

Purpose: ONBD-05 -- dark mode is a foundational visual concern that other plans build on (empty states and skeletons must render correctly in both themes).
Output: ThemeProvider in root layout, ThemeToggle component in sidebar footer, system-preference default.
</objective>

<execution_context>
@/Users/Matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-onboarding-first-impressions/05-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/app/layout.tsx (current):
- RootLayout wraps children in TooltipProvider + Toaster
- Uses Geist + Geist_Mono fonts with CSS variable injection
- html tag has lang="en" but NO suppressHydrationWarning (needed for next-themes)

From src/app/(dashboard)/layout.tsx (current):
- Sidebar uses SidebarHeader, SidebarContent, SidebarGroup -- NO SidebarFooter currently used
- SidebarFooter IS exported from @/components/ui/sidebar (confirmed line 343, 701)
- SidebarProvider wraps Sidebar + SidebarInset
- navItems array defines 4 nav links (Dashboard, Contacts, Drafts, Settings)

From src/components/ui/sonner.tsx:
- Already imports useTheme from "next-themes" and reads theme
- Will work automatically once ThemeProvider wraps the app

From src/app/globals.css:
- .dark {} CSS vars already fully defined (lines 85-117)
- @custom-variant dark (&:is(.dark *)) already configured
- No additional CSS work needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ThemeProvider to root layout and create ThemeToggle component</name>
  <files>src/app/layout.tsx, src/components/theme-toggle.tsx</files>
  <read_first>src/app/layout.tsx, src/components/ui/sonner.tsx</read_first>
  <action>
1. Create src/components/theme-toggle.tsx:
   - "use client" component
   - Import useTheme from "next-themes", Moon and Sun from "lucide-react", Button from "@/components/ui/button"
   - Render a Button variant="ghost" size="icon" that toggles between light/dark/system
   - On click: cycle theme -- if "light" set "dark", if "dark" set "system", if "system" set "light"
   - Show Sun icon when theme is light, Moon when dark, and a Monitor icon (from lucide-react) when system
   - Use `resolvedTheme` for icon display (handles system resolution), `theme` for cycling logic
   - Include aria-label "Toggle theme" for accessibility
   - Add a small text label next to the icon showing current mode: "Light", "Dark", or "System"
   - Component should handle the mounted state to avoid hydration mismatch (common next-themes pattern): useState mounted, useEffect to set mounted=true, return null or placeholder until mounted

2. Update src/app/layout.tsx:
   - Import ThemeProvider from "next-themes"
   - Add suppressHydrationWarning to the html tag (required by next-themes to prevent hydration mismatch)
   - Wrap children + Toaster inside ThemeProvider with props: attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange
   - Keep TooltipProvider inside ThemeProvider
   - Keep existing font variables and metadata unchanged
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && grep -q "ThemeProvider" src/app/layout.tsx && grep -q "suppressHydrationWarning" src/app/layout.tsx && grep -q "useTheme" src/components/theme-toggle.tsx && echo "PASS"</automated>
  </verify>
  <done>
  - ThemeProvider wraps the app in root layout with attribute="class", defaultTheme="system", enableSystem
  - html tag has suppressHydrationWarning
  - ThemeToggle component exists with theme cycling and proper hydration handling
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ThemeToggle to sidebar footer in dashboard layout</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <read_first>src/app/(dashboard)/layout.tsx, src/components/theme-toggle.tsx</read_first>
  <action>
1. Update src/app/(dashboard)/layout.tsx:
   - Add SidebarFooter to the existing destructured imports from "@/components/ui/sidebar"
   - Import ThemeToggle from "@/components/theme-toggle"
   - After the closing SidebarContent tag and before the closing Sidebar tag, add:
     ```
     <SidebarFooter>
       <div className="flex items-center justify-between px-4 py-2">
         <ThemeToggle />
       </div>
     </SidebarFooter>
     ```
   - Keep all existing nav items and structure unchanged
   - This layout is a server component currently -- since ThemeToggle is "use client" and used as a leaf component, the layout can remain a server component (client boundary at ThemeToggle)
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && grep -q "SidebarFooter" src/app/\(dashboard\)/layout.tsx && grep -q "ThemeToggle" src/app/\(dashboard\)/layout.tsx && echo "PASS"</automated>
  </verify>
  <done>
  - SidebarFooter appears in dashboard layout with ThemeToggle inside
  - Toggle is accessible from any dashboard page via sidebar
  - Layout remains a server component; ThemeToggle handles its own client boundary
  </done>
</task>

</tasks>

<verification>
- grep for ThemeProvider in src/app/layout.tsx confirms wrapping
- grep for suppressHydrationWarning on html tag
- grep for SidebarFooter + ThemeToggle in dashboard layout
- npm run build should pass (no type errors)
</verification>

<success_criteria>
- ThemeProvider wraps app with system default
- ThemeToggle in sidebar footer cycles light/dark/system
- Dark mode CSS vars (already defined in globals.css) apply correctly via class="dark"
- Sonner toast theming works automatically (already uses useTheme)
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding-first-impressions/05-01-SUMMARY.md`
</output>

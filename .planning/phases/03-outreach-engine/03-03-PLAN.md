---
phase: 03-outreach-engine
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/app/api/drafts/route.ts
  - src/app/api/drafts/[id]/route.ts
  - src/app/api/drafts/[id]/send/route.ts
  - src/app/(dashboard)/drafts/page.tsx
  - src/components/drafts/draft-card.tsx
  - src/components/drafts/draft-list.tsx
  - src/components/drafts/draft-editor.tsx
  - src/app/(dashboard)/layout.tsx
  - src/__tests__/api/drafts.test.ts
  - src/__tests__/api/drafts-send.test.ts
  - src/__tests__/components/draft-card.test.tsx
autonomous: true
requirements: [OUTR-04, OUTR-05, OUTR-06, OUTR-07, OUTR-08]

must_haves:
  truths:
    - "User can see pending drafts in a list on the drafts page"
    - "User can approve a draft and send it via Gmail from the dashboard"
    - "User can edit a draft subject and body before sending"
    - "User can dismiss a draft, which deletes the Gmail draft and marks it as dismissed"
    - "Draft list shows contact name, subject, generated date, and sync status"
  artifacts:
    - path: "src/app/api/drafts/route.ts"
      provides: "GET list and POST create draft endpoints"
      exports: ["GET", "POST"]
    - path: "src/app/api/drafts/[id]/route.ts"
      provides: "PUT update and DELETE dismiss endpoints"
      exports: ["PUT", "DELETE"]
    - path: "src/app/api/drafts/[id]/send/route.ts"
      provides: "POST approve and send endpoint"
      exports: ["POST"]
    - path: "src/app/(dashboard)/drafts/page.tsx"
      provides: "Draft review dashboard page"
      contains: "DraftList"
    - path: "src/components/drafts/draft-card.tsx"
      provides: "Individual draft display with action buttons"
      contains: "DraftCard"
    - path: "src/components/drafts/draft-editor.tsx"
      provides: "Edit modal for draft subject and body"
      contains: "DraftEditor"
  key_links:
    - from: "src/app/api/drafts/[id]/send/route.ts"
      to: "src/lib/gmail/client.ts"
      via: "sendGmailDraft call"
      pattern: "import.*sendGmailDraft.*from.*gmail/client"
    - from: "src/app/api/drafts/[id]/route.ts"
      to: "src/lib/gmail/client.ts"
      via: "deleteGmailDraft call on dismiss"
      pattern: "import.*deleteGmailDraft.*from.*gmail/client"
    - from: "src/app/(dashboard)/drafts/page.tsx"
      to: "src/components/drafts/draft-list.tsx"
      via: "DraftList component render"
      pattern: "import.*DraftList.*from.*drafts/draft-list"
    - from: "src/components/drafts/draft-list.tsx"
      to: "src/components/drafts/draft-card.tsx"
      via: "maps drafts to DraftCard components"
      pattern: "import.*DraftCard.*from.*draft-card"
---

<objective>
Build the complete human-in-the-loop draft review workflow: API routes for listing, editing, sending, and dismissing drafts; a draft review dashboard page; and draft UI components (card, list, editor). This completes the outreach engine by giving users control over AI-generated drafts.

Purpose: Without this, AI-generated drafts sit in the database unseen. This plan delivers the approve/edit/dismiss workflow that makes the outreach engine usable.
Output: 3 API route files, 1 dashboard page, 3 UI components, 3 test files
</objective>

<execution_context>
@/Users/Matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-outreach-engine/03-RESEARCH.md

<interfaces>
<!-- Interfaces from Plan 01 consumed here -->

From src/lib/gmail/client.ts (created in 03-01):
```typescript
export async function sendGmailDraft(userId: string, gmailDraftId: string): Promise<void>;
export async function deleteGmailDraft(userId: string, gmailDraftId: string): Promise<void>;
export async function createGmailDraft(userId: string, to: string, subject: string, htmlBody: string): Promise<string>;
```

From src/lib/validations/drafts.ts (created in 03-01):
```typescript
export const outreachDraftSchema: z.ZodObject<{ contact_id, subject, body, ai_rationale }>;
export const draftUpdateSchema: z.ZodObject<{ subject?, body? }>;
export const DRAFT_STATUSES: readonly ["pending_review", "approved", "sent", "dismissed"];
export type DraftStatus;
```

<!-- Existing codebase interfaces -->

From src/app/api/contacts/route.ts (established API route pattern):
```typescript
// Pattern: createClient(cookies) -> getUser() -> auth check 401 -> query -> NextResponse.json
import { createClient } from "@/lib/supabase/server";
import { NextResponse } from "next/server";
```

From src/app/api/contacts/[id]/route.ts (established dynamic route pattern):
```typescript
// Pattern: params: Promise<{ id: string }> -> await params -> Zod validate -> query
```

From src/components/contacts/contact-card.tsx (established card component pattern):
```typescript
// Pattern: Card with CardHeader/CardContent, Badge for category, relative dates via formatDistanceToNow
```

From src/app/(dashboard)/contacts/page.tsx (established list page pattern):
```typescript
// Pattern: "use client", fetch on mount with useEffect, loading/empty states, grid layout
```

From src/app/(dashboard)/layout.tsx:
```typescript
// Sidebar navigation items: Dashboard, Contacts, Settings
// Pattern: SidebarMenuButton with render prop, Link with pathname check for isActive
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Draft CRUD and send API routes</name>
  <files>
    src/app/api/drafts/route.ts,
    src/app/api/drafts/[id]/route.ts,
    src/app/api/drafts/[id]/send/route.ts,
    src/__tests__/api/drafts.test.ts,
    src/__tests__/api/drafts-send.test.ts
  </files>
  <read_first>
    src/app/api/contacts/route.ts,
    src/app/api/contacts/[id]/route.ts,
    src/__tests__/api/contacts.test.ts,
    src/lib/validations/drafts.ts,
    src/lib/gmail/client.ts,
    src/lib/supabase/server.ts
  </read_first>
  <behavior>
    - Test 1: GET /api/drafts returns 401 without auth
    - Test 2: GET /api/drafts returns list of user's drafts with contact name joined
    - Test 3: GET /api/drafts?status=pending_review filters by status
    - Test 4: PUT /api/drafts/[id] updates subject and body with Zod validation
    - Test 5: PUT /api/drafts/[id] returns 400 for invalid payload
    - Test 6: DELETE /api/drafts/[id] marks draft as dismissed and calls deleteGmailDraft if gmail_draft_id exists
    - Test 7: DELETE /api/drafts/[id] handles deleteGmailDraft failure gracefully (still dismisses in DB)
    - Test 8: POST /api/drafts/[id]/send returns 401 without auth
    - Test 9: POST /api/drafts/[id]/send calls sendGmailDraft, updates status to sent, updates contact last_interaction_at
    - Test 10: POST /api/drafts/[id]/send returns 400 if draft is not pending_review
    - Test 11: POST /api/drafts/[id]/send creates Gmail draft first if gmail_draft_id is null (failed sync), then sends
  </behavior>
  <action>
    1. Write tests FIRST (RED):

       `src/__tests__/api/drafts.test.ts`:
       - Mock src/lib/supabase/server createClient
       - Mock src/lib/gmail/client (deleteGmailDraft)
       - Follow exact test pattern from src/__tests__/api/contacts.test.ts:
         - Mock getUser to return/reject auth
         - Mock from().select().eq() chains for GET
         - Mock from().update().eq() for PUT
         - Mock from().update().eq() for DELETE (status update to 'dismissed')
       - Test GET /api/drafts: query outreach_drafts with select("*, contacts(name, email)") filtered by user_id
       - Test GET /api/drafts?status=X: additional .eq("status", status) filter
       - Test PUT /api/drafts/[id]: validate with draftUpdateSchema, update subject/body, set updated_at
       - Test DELETE /api/drafts/[id]: update status='dismissed', reviewed_at=now; if gmail_draft_id exists, call deleteGmailDraft (best-effort)

       `src/__tests__/api/drafts-send.test.ts`:
       - Mock src/lib/supabase/server, src/lib/gmail/client (sendGmailDraft, createGmailDraft)
       - Test POST /api/drafts/[id]/send: fetch draft, verify status='pending_review', call sendGmailDraft, update status='sent' and sent_at, update contacts.last_interaction_at

    2. Implement routes (GREEN):

       `src/app/api/drafts/route.ts`:
       - GET: createClient -> getUser -> auth check -> query outreach_drafts with contacts join, optional status filter from searchParams -> return drafts
       - POST: (for manual draft creation if needed later -- stub with 501 Not Implemented for now)

       `src/app/api/drafts/[id]/route.ts`:
       - PUT: auth -> params.id -> validate body with draftUpdateSchema -> update outreach_drafts SET subject, body, updated_at WHERE id AND user_id -> return updated draft
       - DELETE: auth -> params.id -> fetch draft to get gmail_draft_id -> if gmail_draft_id, try deleteGmailDraft (best-effort, catch and log) -> update status='dismissed', reviewed_at=now -> return 200

       `src/app/api/drafts/[id]/send/route.ts`:
       - POST: auth -> params.id -> fetch draft with contact email join -> verify status='pending_review' (else 400) -> if gmail_draft_id is null, create Gmail draft first via createGmailDraft -> call sendGmailDraft -> update outreach_drafts: status='sent', sent_at=now, reviewed_at=now -> update contacts: last_interaction_at=now WHERE id=draft.contact_id -> return 200
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/drafts.test.ts src/__tests__/api/drafts-send.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - GET /api/drafts returns drafts with contact name, supports ?status= filter
    - PUT /api/drafts/[id] updates subject/body with Zod validation
    - DELETE /api/drafts/[id] dismisses draft and best-effort deletes Gmail draft
    - POST /api/drafts/[id]/send sends via Gmail, updates status to sent, updates contact last_interaction_at
    - Send creates Gmail draft first if gmail_draft_id is null (handles failed sync recovery)
    - All 11 tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Draft review dashboard page and UI components</name>
  <files>
    src/components/drafts/draft-card.tsx,
    src/components/drafts/draft-list.tsx,
    src/components/drafts/draft-editor.tsx,
    src/app/(dashboard)/drafts/page.tsx,
    src/app/(dashboard)/layout.tsx,
    src/__tests__/components/draft-card.test.tsx
  </files>
  <read_first>
    src/components/contacts/contact-card.tsx,
    src/app/(dashboard)/contacts/page.tsx,
    src/app/(dashboard)/layout.tsx,
    src/__tests__/components/contact-card.test.tsx,
    src/lib/validations/drafts.ts
  </read_first>
  <action>
    1. Create `src/components/drafts/draft-card.tsx`:
       - "use client"
       - Props: draft object (id, subject, body, status, gmail_sync_status, ai_rationale, generated_at, contacts: { name, email }), onSend callback, onDismiss callback, onEdit callback
       - Use Card from shadcn (CardHeader, CardContent, CardFooter)
       - CardHeader: contact name (bold), email (muted), subject line
       - CardContent: body preview (first 200 chars, strip HTML tags for preview, dangerouslySetInnerHTML in expanded view), ai_rationale in a muted collapsible section
       - Gmail sync status indicator: green dot for "synced", yellow for "pending", red for "failed" with text label
       - Generated date: formatDistanceToNow from date-fns with { addSuffix: true }
       - CardFooter: 3 action buttons (only show for pending_review status):
         - Send (Mail/Send icon, primary variant) -> calls onSend(draft.id)
         - Edit (Pencil icon, outline variant) -> calls onEdit(draft)
         - Dismiss (Trash2 icon, ghost variant with destructive text) -> calls onDismiss(draft.id)
       - For sent/dismissed status: show status badge instead of action buttons

    2. Create `src/components/drafts/draft-editor.tsx`:
       - "use client"
       - Props: draft (id, subject, body) | null, open boolean, onClose callback, onSave callback
       - Use Sheet from shadcn (SheetContent, SheetHeader, SheetTitle, SheetFooter)
       - React Hook Form with zodResolver and draftUpdateSchema
       - Input for subject field
       - Textarea for body field (large, ~10 rows)
       - Save button calls onSave({ subject, body }) then onClose
       - Cancel button calls onClose

    3. Create `src/components/drafts/draft-list.tsx`:
       - "use client"
       - Props: drafts array, loading boolean, onSend callback, onDismiss callback, onEdit callback
       - Maps drafts to DraftCard components
       - Loading state: 3 skeleton cards (matching contact list skeleton pattern)
       - Empty state: "No drafts to review" with Mail icon and descriptive text

    4. Create `src/app/(dashboard)/drafts/page.tsx`:
       - "use client"
       - Fetch drafts on mount: GET /api/drafts?status=pending_review
       - State: drafts array, loading boolean, editingDraft (for editor sheet)
       - Status filter tabs or Select: "Pending Review", "Sent", "Dismissed", "All" -- fetch with corresponding ?status= param
       - handleSend: POST /api/drafts/[id]/send -> toast success "Email sent" -> remove from list
       - handleDismiss: DELETE /api/drafts/[id] -> toast "Draft dismissed" -> remove from list
       - handleEdit: open DraftEditor sheet with selected draft
       - handleSaveEdit: PUT /api/drafts/[id] with { subject, body } -> toast "Draft updated" -> update in list -> close editor
       - Render DraftList + DraftEditor
       - Title: "Draft Review" with count badge

    5. Update `src/app/(dashboard)/layout.tsx`:
       - Add "Drafts" navigation item between "Contacts" and "Settings"
       - Use Mail icon from lucide-react
       - href="/drafts", isActive check for pathname.startsWith("/drafts")

    6. Write tests in `src/__tests__/components/draft-card.test.tsx`:
       - Test renders contact name and subject
       - Test renders action buttons for pending_review status
       - Test does NOT render action buttons for sent status
       - Test renders gmail sync status indicator
       - Test calls onSend when Send button clicked
       - Use getAllByText pattern if needed (base-ui duplicate rendering, established in Phase 2)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/draft-card.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - DraftCard renders contact info, subject, body preview, sync status, action buttons
    - DraftEditor opens as Sheet with subject/body form, saves via onSave callback
    - DraftList maps drafts to cards with loading/empty states
    - Drafts page at /drafts fetches and displays drafts, handles send/dismiss/edit actions with toast feedback
    - Sidebar shows Drafts nav item between Contacts and Settings
    - All DraftCard tests pass
    - Full test suite: npx vitest run passes
  </done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
All new tests (draft API, draft send, draft card) pass alongside all existing tests.
Drafts page accessible at /drafts from sidebar navigation.
Full send/edit/dismiss workflow functional via API routes.
</verification>

<success_criteria>
- Draft API routes support list (with status filter), update, dismiss, and send
- Send endpoint calls sendGmailDraft and updates contact last_interaction_at
- Dismiss endpoint best-effort deletes Gmail draft
- Drafts page shows pending drafts with send/edit/dismiss actions
- Edit opens Sheet with form, saves via PUT
- Sidebar includes Drafts navigation
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-outreach-engine/03-03-SUMMARY.md`
</output>

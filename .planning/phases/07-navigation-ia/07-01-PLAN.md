---
phase: 07-navigation-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/breadcrumb.tsx
  - src/components/navigation/breadcrumbs.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/contacts/page.tsx
  - src/app/(dashboard)/contacts/[id]/page.tsx
  - src/app/(dashboard)/drafts/page.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/app/(dashboard)/settings/profile/page.tsx
  - src/app/(dashboard)/settings/integrations/page.tsx
  - src/app/(dashboard)/settings/schedule/page.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-06

must_haves:
  truths:
    - "Every dashboard page shows a top header bar with sidebar trigger button, breadcrumbs, and theme toggle"
    - "Breadcrumbs update dynamically showing the current page location with clickable segments"
    - "Contact detail page breadcrumb shows 'Contacts > Contact Name' with Contacts as a link"
    - "Settings subpages show 'Settings > Profile/Integrations/Schedule' with Settings as a link"
    - "All page headings use consistent typography hierarchy (h1 for page titles, p for descriptions)"
    - "Theme toggle has been moved from sidebar footer to the header bar"
  artifacts:
    - path: "src/components/ui/breadcrumb.tsx"
      provides: "shadcn breadcrumb primitives"
    - path: "src/components/navigation/breadcrumbs.tsx"
      provides: "Dynamic breadcrumb component using pathname"
      exports: ["Breadcrumbs"]
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Header bar in SidebarInset with SidebarTrigger + Breadcrumbs + ThemeToggle"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/navigation/breadcrumbs.tsx"
      via: "import Breadcrumbs"
      pattern: "import.*Breadcrumbs.*from.*navigation/breadcrumbs"
    - from: "src/components/navigation/breadcrumbs.tsx"
      to: "src/components/ui/breadcrumb.tsx"
      via: "import breadcrumb primitives"
      pattern: "import.*from.*ui/breadcrumb"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/theme-toggle.tsx"
      via: "ThemeToggle in header instead of sidebar footer"
      pattern: "ThemeToggle"
---

<objective>
Build the dashboard header bar with dynamic breadcrumbs and standardize typography across all pages.

Purpose: Users can orient themselves at any depth in the app via breadcrumbs in a persistent header bar. Typography is consistent across all pages.
Output: Header bar component in layout, dynamic breadcrumb component, standardized page headings.
</objective>

<execution_context>
@/Users/Matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/app/(dashboard)/layout.tsx (current structure):
```tsx
// SidebarProvider > Sidebar > SidebarInset > main
// ThemeToggle currently in SidebarFooter -- must move to header
// SidebarTrigger available from sidebar.tsx as exported component
```

From src/components/ui/sidebar.tsx:
```tsx
export function SidebarTrigger({ className, onClick, ...props }: React.ComponentProps<typeof Button>)
// Renders a Button with PanelLeftIcon that calls toggleSidebar()
// Also exports: SidebarInset, SidebarProvider, Sidebar, etc.
```

From src/components/theme-toggle.tsx:
```tsx
export function ThemeToggle()
// Three-way cycle: light -> dark -> system
// Uses useTheme() from next-themes with mounted state guard
```

Dashboard route structure (all need consistent headings):
- /dashboard -- "Dashboard" h1 (currently text-3xl font-bold)
- /contacts -- "Contacts" h1 (currently text-3xl font-bold)
- /contacts/[id] -- no h1 (ContactDetail component handles it)
- /drafts -- "Draft Review" h1 (currently text-3xl font-bold)
- /settings -- "Settings" h1 (currently text-2xl font-bold) <-- inconsistent
- /settings/profile -- needs check
- /settings/integrations -- "Integrations" h1 (currently text-2xl font-bold)
- /settings/schedule -- needs check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn breadcrumb, create dynamic Breadcrumbs component, build header bar in layout</name>
  <read_first>
    - src/app/(dashboard)/layout.tsx
    - src/components/ui/sidebar.tsx (lines 254-280 for SidebarTrigger export)
    - src/components/theme-toggle.tsx
    - src/components/ui/tooltip.tsx (for base-ui pattern reference)
  </read_first>
  <files>
    src/components/ui/breadcrumb.tsx,
    src/components/navigation/breadcrumbs.tsx,
    src/app/(dashboard)/layout.tsx
  </files>
  <action>
1. Install shadcn breadcrumb component:
   ```bash
   npx shadcn@latest add breadcrumb --yes
   ```
   This creates src/components/ui/breadcrumb.tsx with Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator exports.

2. Create src/components/navigation/breadcrumbs.tsx -- a "use client" component that:
   - Uses `usePathname()` from `next/navigation` to get current path
   - Parses pathname segments into breadcrumb items
   - Maps known segments to display labels:
     - "dashboard" -> "Dashboard"
     - "contacts" -> "Contacts"
     - "drafts" -> "Drafts"
     - "settings" -> "Settings"
     - "profile" -> "Profile"
     - "integrations" -> "Integrations"
     - "schedule" -> "Schedule"
   - For contact detail pages (/contacts/[id]), fetches contact name from /api/contacts/[id] and displays "Contacts > {name}" (show id as fallback while loading)
   - Each segment except the last is a clickable BreadcrumbLink using next/link
   - Last segment uses BreadcrumbPage (non-clickable, current page)
   - Uses Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator from ui/breadcrumb
   - Uses Slash from lucide-react as separator icon inside BreadcrumbSeparator

3. Modify src/app/(dashboard)/layout.tsx:
   - Remove ThemeToggle from SidebarFooter (remove entire SidebarFooter if nothing else is in it)
   - Add a header bar inside SidebarInset, ABOVE the main content:
     ```tsx
     <SidebarInset>
       <header className="flex h-14 shrink-0 items-center gap-2 border-b px-4">
         <SidebarTrigger />
         <Separator orientation="vertical" className="mr-2 h-4" />
         <Breadcrumbs />
         <div className="ml-auto">
           <ThemeToggle />
         </div>
       </header>
       <main className="flex-1 p-6">{children}</main>
     </SidebarInset>
     ```
   - Import Separator from "@/components/ui/separator"
   - Import Breadcrumbs from "@/components/navigation/breadcrumbs"
   - Import SidebarTrigger (already imported)
   - Keep ThemeToggle import but change usage location
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Header bar visible at top of every dashboard page with SidebarTrigger on left, breadcrumbs in middle, ThemeToggle on right
    - ThemeToggle removed from sidebar footer
    - Breadcrumbs show dynamic path segments based on current URL
    - Contact detail pages show "Contacts > {Contact Name}"
    - Settings subpages show "Settings > {Subpage Name}"
    - grep confirms: `import.*Breadcrumbs.*from.*navigation/breadcrumbs` in layout.tsx
    - grep confirms: `SidebarTrigger` in header element in layout.tsx
  </done>
</task>

<task type="auto">
  <name>Task 2: Standardize typography hierarchy across all dashboard pages</name>
  <read_first>
    - src/app/(dashboard)/dashboard/page.tsx
    - src/app/(dashboard)/contacts/page.tsx
    - src/app/(dashboard)/drafts/page.tsx
    - src/app/(dashboard)/settings/page.tsx
    - src/app/(dashboard)/settings/profile/page.tsx
    - src/app/(dashboard)/settings/integrations/page.tsx
    - src/app/(dashboard)/settings/schedule/page.tsx
  </read_first>
  <files>
    src/app/(dashboard)/settings/page.tsx,
    src/app/(dashboard)/settings/integrations/page.tsx,
    src/app/(dashboard)/settings/profile/page.tsx,
    src/app/(dashboard)/settings/schedule/page.tsx
  </files>
  <action>
Standardize all page headings to use consistent typography:
- Page title h1: `text-2xl font-semibold tracking-tight` (consistent across ALL pages)
- Page description p: `text-sm text-muted-foreground` (consistent across ALL pages)

Since breadcrumbs now provide navigation context in the header, page titles can be slightly smaller (text-2xl instead of text-3xl) to create proper visual hierarchy under the header.

For each page, update the heading pattern to match:
```tsx
<h1 className="text-2xl font-semibold tracking-tight">{Page Title}</h1>
<p className="text-sm text-muted-foreground">{description}</p>
```

Pages to update (check each and only modify those that differ from the standard):
- dashboard/page.tsx: change text-3xl font-bold -> text-2xl font-semibold
- contacts/page.tsx: change text-3xl font-bold -> text-2xl font-semibold
- drafts/page.tsx: change text-3xl font-bold -> text-2xl font-semibold
- settings/page.tsx: already text-2xl, change font-bold -> font-semibold
- settings/profile/page.tsx: check and standardize
- settings/integrations/page.tsx: already text-2xl, change font-bold -> font-semibold
- settings/schedule/page.tsx: check and standardize

Do NOT modify src/app/(dashboard)/contacts/[id]/page.tsx -- the ContactDetail component manages its own heading and that is a client component with complex layout.
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && grep -r "text-2xl font-semibold tracking-tight" src/app/\(dashboard\)/*/page.tsx src/app/\(dashboard\)/settings/*/page.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    - All dashboard page h1 elements use "text-2xl font-semibold tracking-tight"
    - All page description p elements use "text-sm text-muted-foreground"
    - No page uses text-3xl for its heading
    - grep for "text-3xl" in dashboard page files returns 0 results (excluding skeleton/loading states and contact-detail component)
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. Header bar renders on every page with SidebarTrigger, breadcrumbs, theme toggle
3. Breadcrumbs show correct path for each page:
   - /dashboard -> "Dashboard"
   - /contacts -> "Contacts"
   - /contacts/abc-123 -> "Contacts > {Contact Name}"
   - /drafts -> "Drafts"
   - /settings -> "Settings"
   - /settings/integrations -> "Settings > Integrations"
4. All page headings consistent: text-2xl font-semibold tracking-tight
5. ThemeToggle no longer in sidebar footer
</verification>

<success_criteria>
- Header bar with SidebarTrigger + breadcrumbs + ThemeToggle appears on all dashboard pages
- Breadcrumbs are dynamic and update on navigation
- Each breadcrumb segment except the last is a clickable link
- Contact detail breadcrumb resolves contact name
- Typography is consistent across all pages (text-2xl font-semibold)
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-ia/07-01-SUMMARY.md`
</output>

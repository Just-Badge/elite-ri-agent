---
phase: 09-code-quality-devops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/__tests__/components/draft-editor.test.tsx
  - src/__tests__/api/meetings-process.test.ts
  - src/__tests__/trigger/generate-user-drafts.test.ts
  - src/__tests__/contacts/dedup.test.ts
  - src/__tests__/contacts/meetings.test.ts
  - src/components/contacts/contact-form.tsx
  - src/components/contacts/contact-detail.tsx
  - src/app/(dashboard)/drafts/page.tsx
  - src/trigger/generate-user-drafts.ts
  - src/trigger/meeting-dispatcher.ts
  - src/trigger/outreach-dispatcher.ts
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "`npx tsc --noEmit` exits with code 0 (zero errors)"
    - "`npm run build` succeeds without ignoreBuildErrors escape hatch"
    - "No `any` type annotations remain in src/ files (excluding node_modules)"
  artifacts:
    - path: "next.config.ts"
      provides: "Clean Next.js config without ignoreBuildErrors or ignoreDuringBuilds"
      contains: "output: \"standalone\""
    - path: "src/trigger/meeting-dispatcher.ts"
      provides: "Fixed queue option type for Trigger.dev v4"
    - path: "src/trigger/outreach-dispatcher.ts"
      provides: "Fixed queue option type for Trigger.dev v4"
  key_links:
    - from: "next.config.ts"
      to: "tsconfig.json"
      via: "Build pipeline uses strict TS checking"
      pattern: "strict.*true"
    - from: "src/__tests__/components/draft-editor.test.tsx"
      to: "src/components/drafts/draft-editor.tsx"
      via: "Mock functions match DraftEditorProps types"
      pattern: "vi\\.fn<"
---

<objective>
Fix all TypeScript strict mode errors and remove build escape hatches so `npm run build` passes with real type checking.

Purpose: The codebase already has `strict: true` in tsconfig.json but next.config.ts bypasses errors with `ignoreBuildErrors: true` and `ignoreDuringBuilds: true`. There are 9 source files with type errors (20 total errors) and 2 test files using `any`. This plan fixes every error and removes the escape hatches.

Output: Zero TypeScript errors, clean build, no `any` types in src/.
</objective>

<execution_context>
@/Users/Matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all TypeScript errors across 9 source files</name>
  <files>
    src/__tests__/components/draft-editor.test.tsx,
    src/__tests__/api/meetings-process.test.ts,
    src/__tests__/trigger/generate-user-drafts.test.ts,
    src/__tests__/contacts/dedup.test.ts,
    src/__tests__/contacts/meetings.test.ts,
    src/components/contacts/contact-form.tsx,
    src/components/contacts/contact-detail.tsx,
    src/app/(dashboard)/drafts/page.tsx,
    src/trigger/generate-user-drafts.ts,
    src/trigger/meeting-dispatcher.ts,
    src/trigger/outreach-dispatcher.ts
  </files>
  <read_first>
    src/__tests__/components/draft-editor.test.tsx,
    src/__tests__/api/meetings-process.test.ts,
    src/__tests__/trigger/generate-user-drafts.test.ts,
    src/__tests__/contacts/dedup.test.ts,
    src/__tests__/contacts/meetings.test.ts,
    src/components/contacts/contact-form.tsx,
    src/components/contacts/contact-detail.tsx,
    src/app/(dashboard)/drafts/page.tsx,
    src/trigger/generate-user-drafts.ts,
    src/trigger/meeting-dispatcher.ts,
    src/trigger/outreach-dispatcher.ts,
    src/components/drafts/draft-editor.tsx,
    src/lib/validations/contacts.ts,
    src/app/api/meetings/process/route.ts
  </read_first>
  <action>
Fix each file's TypeScript errors. Here are the specific fixes needed:

**Test files:**

1. `src/__tests__/components/draft-editor.test.tsx` (10 errors) — The `vi.fn()` mocks don't match `DraftEditorProps.onClose: () => void` and `onSave: (data: DraftUpdateData) => void`. Fix: type the mocks properly with `vi.fn<() => void>()` for onClose and `vi.fn<(data: { subject?: string; body?: string }) => void>()` for onSave. Apply to all 5 render calls.

2. `src/__tests__/api/meetings-process.test.ts` (2 errors) — `POST` is called with `new Request(...)` argument but the route's `POST()` takes zero parameters. Fix: call `POST()` with no arguments. The route doesn't use the request object.

3. `src/__tests__/trigger/generate-user-drafts.test.ts` (1 error) — A mock is being called but TypeScript can't resolve the call signature. Find the line (around line 384) and cast the mock appropriately, e.g., `(mockFn as ReturnType<typeof vi.fn>)(...)` or use `vi.mocked()`.

**Source files:**

4. `src/components/contacts/contact-form.tsx` (2 errors) — The `contactSchema` has `status` with `.default("active")` and `outreach_frequency_days` with `.default(30)`, making the inferred input type have those as optional. But `useForm<ContactFormValues>` uses the OUTPUT type where they're required. Fix: use `z.input<typeof contactSchema>` for the form's generic type parameter instead of `z.infer<typeof contactSchema>`, OR add a separate `ContactFormInput` type. The simplest fix: in `src/lib/validations/contacts.ts`, export a `ContactFormInput` type using `z.input<typeof contactSchema>` and use that as the useForm generic. Then cast the onSave handler appropriately since handleSubmit outputs the parsed (complete) type.

5. `src/components/contacts/contact-detail.tsx` (1 error, line 304) — `contact.status ?? "active"` produces `string` but the form expects `"active" | "not_pursuing" | "dormant"`. Fix: cast it: `(contact.status as "active" | "not_pursuing" | "dormant") ?? "active"` — or better, add a type assertion using the status union type.

6. `src/app/(dashboard)/drafts/page.tsx` (1 error, line 156) — `Select.onValueChange` receives `(value: string | null, ...) => void` but `setStatusFilter` is `Dispatch<SetStateAction<string>>` which doesn't accept null. Fix: wrap in a handler: `onValueChange={(val) => setStatusFilter(val ?? "")}`.

7. `src/trigger/generate-user-drafts.ts` (2 errors) — Line 152: type predicate `m is { title: string; summary?: string; date: string }` has optional `summary` but the array element type has `summary: string | undefined` (required). Fix: make the predicate type match by using `summary: string | undefined` in the predicate. Line 193: the `.filter()` result still contains nulls. Fix: chain `.filter((m): m is NonNullable<typeof m> => m !== null)` properly, or use `.flatMap()` to handle nulls.

8. `src/trigger/meeting-dispatcher.ts` (1 error, line 93) and `src/trigger/outreach-dispatcher.ts` (1 error, line 79) — The `queue` option `{ name: string; concurrencyLimit: number }` is being passed where `string` is expected. This is a Trigger.dev v4 SDK typing issue. Fix: use the `queue` import from `@trigger.dev/sdk` to create a queue object, OR pass queue as a separate trigger option. Check the Trigger.dev v4 API: the correct pattern for inline queue in `.trigger()` options may need `concurrencyLimit` at the trigger options level, not nested. Read the actual type definition in node_modules to determine the correct shape. The CLAUDE.md docs show `queue: { name: "...", concurrencyLimit: N }` is valid for task-level config but may differ for trigger options. If the SDK expects a string queue name in trigger options, change to just passing the queue name string.

**any types in test files:**

9. `src/__tests__/contacts/dedup.test.ts` — 5 occurrences of `: any`. Replace each with the proper type (e.g., `unknown`, or the specific type from the function signatures being tested).

10. `src/__tests__/contacts/meetings.test.ts` — 3 occurrences of `: any`. Replace each with proper types.

After all fixes, run `npx tsc --noEmit` to verify zero errors. If new errors appear from fixes, resolve them.
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0 with no errors
    - `grep -r ": any\b" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules` returns no matches
    - `npx vitest run` still passes all 188 tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove ignoreBuildErrors and ignoreDuringBuilds from next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>next.config.ts</read_first>
  <action>
Remove the `typescript.ignoreBuildErrors: true` and `eslint.ignoreDuringBuilds: true` blocks from next.config.ts. The resulting config should only contain:

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "standalone",
};

export default nextConfig;
```

Then run `npm run build` to verify the build passes without the escape hatches. This MUST be done AFTER Task 1 fixes all TS errors, otherwise the build will fail.
  </action>
  <verify>
    <automated>cd /Users/Matthew/Projects/elite-ri-agent && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - next.config.ts has no `ignoreBuildErrors` or `ignoreDuringBuilds` properties
    - `npm run build` exits 0
    - `grep -n "ignoreBuildErrors\|ignoreDuringBuilds" next.config.ts` returns no matches
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- `npx vitest run` passes all tests (188)
- `grep -rn ": any" src/ --include="*.ts" --include="*.tsx"` returns no matches
- `grep "ignoreBuildErrors\|ignoreDuringBuilds" next.config.ts` returns no matches
</verification>

<success_criteria>
TypeScript strict mode is fully enforced with zero build errors, no escape hatches, and no `any` types in the codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/09-code-quality-devops/09-01-SUMMARY.md`
</output>

---
phase: 04-dashboard-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/contacts/risk.ts
  - src/__tests__/contacts/risk.test.ts
  - src/app/api/dashboard/stats/route.ts
  - src/__tests__/api/dashboard-stats.test.ts
  - src/app/api/dashboard/analytics/route.ts
  - src/__tests__/api/dashboard-analytics.test.ts
  - src/app/api/contacts/route.ts
  - src/app/api/contacts/[id]/route.ts
autonomous: true
requirements: [DASH-02, DASH-04, DASH-05, DASH-06, DASH-07]

must_haves:
  truths:
    - "computeContactRisk returns correct days_overdue and risk_level for overdue, healthy, and unknown contacts"
    - "Dashboard stats API returns at-risk contacts, triage contacts, pending action items, and draft summary in one response"
    - "Dashboard analytics API returns draft counts grouped by month for charting"
    - "Contacts search API matches on category text in addition to name, email, company, notes"
    - "Editing a contact sets ai_confidence to manual, removing it from triage queue"
  artifacts:
    - path: "src/lib/contacts/risk.ts"
      provides: "Risk computation utility"
      exports: ["computeContactRisk", "RiskLevel", "ContactRisk"]
    - path: "src/app/api/dashboard/stats/route.ts"
      provides: "Dashboard stats aggregation endpoint"
      exports: ["GET"]
    - path: "src/app/api/dashboard/analytics/route.ts"
      provides: "Outreach analytics time-series endpoint"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/dashboard/stats/route.ts"
      to: "src/lib/contacts/risk.ts"
      via: "import computeContactRisk"
      pattern: "computeContactRisk"
    - from: "src/app/api/dashboard/stats/route.ts"
      to: "supabase contacts, action_items, outreach_drafts"
      via: "Promise.all parallel queries"
      pattern: "Promise\\.all"
    - from: "src/app/api/contacts/[id]/route.ts"
      to: "contacts.ai_confidence"
      via: "set ai_confidence: 'manual' on PUT"
      pattern: "ai_confidence.*manual"
---

<objective>
Build the backend data layer for Phase 4 dashboard intelligence: a shared risk computation utility, two dashboard API endpoints (stats aggregation and analytics time-series), enhanced contacts search to include category, and triage exit mechanism via ai_confidence update on contact edit.

Purpose: All dashboard UI components in Plan 02 depend on these API endpoints and the risk utility. Building the data layer first ensures Plan 02 can focus purely on UI without needing to design data contracts.

Output: Risk utility at src/lib/contacts/risk.ts, dashboard stats API at /api/dashboard/stats, analytics API at /api/dashboard/analytics, enhanced contacts search, and triage exit on contact edit.
</objective>

<execution_context>
@/Users/Matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-intelligence/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/validations/contacts.ts:
```typescript
export const CONTACT_CATEGORIES = [
  "accelerators", "advisors", "clients", "investors",
  "networking", "partners", "potential-team", "team",
] as const;
export const contactSchema = z.object({ ... });
export const contactUpdateSchema = contactSchema.partial().extend({ id: z.string().uuid() });
export type ContactFormValues = z.infer<typeof contactSchema>;
export type ContactUpdateValues = z.infer<typeof contactUpdateSchema>;
```

Database schema (contacts table):
```sql
-- Key columns for risk/triage:
outreach_frequency_days INTEGER DEFAULT 30,
last_interaction_at TIMESTAMPTZ,
ai_confidence TEXT CHECK (ai_confidence IN ('high', 'medium', 'low', 'manual')),
status TEXT DEFAULT 'active' CHECK (status IN ('active', 'not_pursuing', 'dormant')),
created_at TIMESTAMPTZ DEFAULT NOW(),
updated_at TIMESTAMPTZ DEFAULT NOW()
```

Database schema (action_items table):
```sql
contact_id UUID NOT NULL REFERENCES contacts(id) ON DELETE CASCADE,
text TEXT NOT NULL,
completed BOOLEAN DEFAULT false
```

Database schema (outreach_drafts table):
```sql
status TEXT DEFAULT 'pending_review' CHECK (status IN ('pending_review', 'approved', 'sent', 'dismissed')),
sent_at TIMESTAMPTZ,
created_at TIMESTAMPTZ DEFAULT NOW()
```

Existing API route pattern (src/app/api/contacts/route.ts):
```typescript
const supabase = await createClient();
const { data: { user } } = await supabase.auth.getUser();
if (!user) return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

Existing test pattern (src/__tests__/api/contacts.test.ts):
```typescript
// vi.hoisted for mock variables, chainable query builder, setAuthenticated helper
const { mockFrom, mockAuth, mockSupabase } = vi.hoisted(() => { ... });
vi.mock("@/lib/supabase/server", () => ({
  createClient: vi.fn().mockResolvedValue(mockSupabase),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Risk computation utility with tests</name>
  <files>src/lib/contacts/risk.ts, src/__tests__/contacts/risk.test.ts</files>
  <read_first>
    - src/lib/validations/contacts.ts (contact types and field names)
    - src/__tests__/contacts/dedup.test.ts (test pattern reference for contacts domain tests)
  </read_first>
  <behavior>
    - computeContactRisk({ outreach_frequency_days: 30, last_interaction_at: 45 days ago }) -> { days_overdue: 15, is_at_risk: true, risk_level: "warning" }
    - computeContactRisk({ outreach_frequency_days: 30, last_interaction_at: 70 days ago }) -> { days_overdue: 40, is_at_risk: true, risk_level: "critical" } (overdue > frequency = critical)
    - computeContactRisk({ outreach_frequency_days: 30, last_interaction_at: 10 days ago }) -> { days_overdue: 0, is_at_risk: false, risk_level: "healthy" }
    - computeContactRisk({ outreach_frequency_days: null, last_interaction_at: null, created_at: null }) -> { days_overdue: 0, is_at_risk: false, risk_level: "unknown" }
    - computeContactRisk({ outreach_frequency_days: null, last_interaction_at: null, created_at: 5 days ago }) -> uses created_at as fallback, defaults frequency to 30, risk_level: "healthy"
    - computeContactRisk({ outreach_frequency_days: 7, last_interaction_at: null, created_at: 20 days ago }) -> uses created_at, frequency 7, days_overdue: 13, risk_level: "critical"
  </behavior>
  <action>
    Create src/lib/contacts/risk.ts:

    Export types:
    - `RiskLevel = "critical" | "warning" | "healthy" | "unknown"`
    - `ContactRisk = { days_overdue: number; is_at_risk: boolean; risk_level: RiskLevel }`

    Export function `computeContactRisk(contact)` that accepts `{ outreach_frequency_days?: number | null; last_interaction_at?: string | null; created_at?: string | null }`:
    1. Default frequency to 30 if null/undefined
    2. Use last_interaction_at if available, else created_at as reference date
    3. If no reference date at all, return { days_overdue: 0, is_at_risk: false, risk_level: "unknown" }
    4. Compute daysSince = differenceInDays(new Date(), new Date(referenceDate))
    5. Compute daysOverdue = daysSince - frequency
    6. risk_level: daysOverdue > frequency -> "critical", daysOverdue > 0 -> "warning", else "healthy"
    7. Return { days_overdue: Math.max(0, daysOverdue), is_at_risk: daysOverdue > 0, risk_level }

    Use `differenceInDays` from `date-fns` (already in project).

    Write tests FIRST in src/__tests__/contacts/risk.test.ts covering all 6 behavior cases above. Use fixed dates via vi.useFakeTimers for deterministic testing.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contacts/risk.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - src/lib/contacts/risk.ts exports computeContactRisk, RiskLevel, ContactRisk
    - All 6 test cases pass: overdue-warning, overdue-critical, healthy, unknown, created_at-fallback, custom-frequency
    - `grep -c "export" src/lib/contacts/risk.ts` returns at least 3 (function + 2 types)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard stats and analytics API endpoints with tests</name>
  <files>src/app/api/dashboard/stats/route.ts, src/app/api/dashboard/analytics/route.ts, src/__tests__/api/dashboard-stats.test.ts, src/__tests__/api/dashboard-analytics.test.ts</files>
  <read_first>
    - src/lib/contacts/risk.ts (risk utility created in Task 1)
    - src/__tests__/api/contacts.test.ts (test mocking pattern: vi.hoisted, chainable query builder, setAuthenticated)
    - src/app/api/contacts/route.ts (API route pattern: createClient, getUser guard)
    - src/__tests__/api/drafts.test.ts (drafts API test patterns)
  </read_first>
  <behavior>
    Dashboard Stats (GET /api/dashboard/stats):
    - Returns 401 when unauthenticated
    - Returns { data: { at_risk_contacts, triage_contacts, pending_actions, draft_stats, summary } }
    - at_risk_contacts: contacts where computeContactRisk returns is_at_risk=true, sorted by days_overdue desc
    - triage_contacts: contacts where ai_confidence != 'manual' (AI-created, not yet reviewed)
    - pending_actions: action_items where completed=false, joined with contact name
    - draft_stats: { total, sent, pending, dismissed } counts from outreach_drafts
    - summary: { total_contacts, at_risk_count, triage_count, pending_actions_count }

    Dashboard Analytics (GET /api/dashboard/analytics):
    - Returns 401 when unauthenticated
    - Accepts ?period=7d|30d|90d|all query param (default 30d)
    - Returns { data: { by_month: [{ month: "2026-01", sent: N, dismissed: N, pending: N, total: N }], totals: { sent, dismissed, pending, total } } }
    - Groups outreach_drafts by month of created_at
    - Counts by status: sent, dismissed, pending_review+approved (as pending)
  </behavior>
  <action>
    Write tests FIRST following the vi.hoisted + chainable query builder pattern from src/__tests__/api/contacts.test.ts.

    For dashboard-stats.test.ts:
    - Mock supabase.from("contacts").select("*") to return test contacts with varying outreach_frequency_days and last_interaction_at
    - Mock supabase.from("action_items").select("*, contacts(name)").eq("completed", false) to return incomplete actions
    - Mock supabase.from("outreach_drafts").select("status, sent_at, created_at") to return drafts with mixed statuses
    - The mock must handle Promise.all -- each from() call returns a different chain based on the table name argument
    - Test: unauthenticated returns 401
    - Test: authenticated returns correct at_risk_contacts (filter contacts through computeContactRisk)
    - Test: triage_contacts filters where ai_confidence != 'manual'
    - Test: pending_actions returns only incomplete items
    - Test: draft_stats counts are accurate

    For dashboard-analytics.test.ts:
    - Mock outreach_drafts query results with drafts spread across months
    - Test: default period is 30d
    - Test: by_month aggregation groups correctly
    - Test: totals match individual counts

    Create GET /api/dashboard/stats/route.ts:
    - Auth guard pattern: createClient() -> getUser() -> 401 if no user
    - Run 3 parallel queries via Promise.all:
      1. contacts: supabase.from("contacts").select("id, name, email, company, category, outreach_frequency_days, last_interaction_at, ai_confidence, status, created_at").eq("user_id", user.id)
      2. action_items: supabase.from("action_items").select("id, text, completed, contact_id, contacts(name)").eq("user_id", user.id).eq("completed", false)
      3. outreach_drafts: supabase.from("outreach_drafts").select("status, sent_at, created_at").eq("user_id", user.id)
    - Map contacts through computeContactRisk to add risk fields
    - Filter at_risk = contacts where is_at_risk === true, sorted by days_overdue desc
    - Filter triage = contacts where ai_confidence !== "manual"
    - Compute draft_stats by counting statuses
    - Return structured response with data wrapper

    Create GET /api/dashboard/analytics/route.ts:
    - Auth guard pattern
    - Parse ?period query param (default "30d"), compute start date: 7d/30d/90d from now, or no filter for "all"
    - Query outreach_drafts with optional .gte("created_at", startDate)
    - Group results by month (YYYY-MM format from created_at)
    - Count per month: sent (status=sent), dismissed (status=dismissed), pending (status in pending_review, approved)
    - Return { data: { by_month: [...], totals: {...} } }
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/dashboard-stats.test.ts src/__tests__/api/dashboard-analytics.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - GET /api/dashboard/stats returns aggregated dashboard data with risk, triage, actions, drafts
    - GET /api/dashboard/analytics returns time-series draft data grouped by month
    - `grep -c "export async function GET" src/app/api/dashboard/stats/route.ts` returns 1
    - `grep -c "export async function GET" src/app/api/dashboard/analytics/route.ts` returns 1
    - `grep "computeContactRisk" src/app/api/dashboard/stats/route.ts` finds import usage
    - `grep "Promise.all" src/app/api/dashboard/stats/route.ts` confirms parallel queries
    - All test cases pass for both endpoints
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance contacts search and triage exit</name>
  <files>src/app/api/contacts/route.ts, src/app/api/contacts/[id]/route.ts, src/__tests__/api/contacts.test.ts</files>
  <read_first>
    - src/app/api/contacts/route.ts (current search .or() clause)
    - src/app/api/contacts/[id]/route.ts (current PUT handler)
    - src/__tests__/api/contacts.test.ts (existing test cases to extend)
  </read_first>
  <action>
    Two targeted changes:

    1. DASH-02 fix: In GET /api/contacts route.ts, add `category` to the .or() ilike search clause.
       Current: `name.ilike.%${search}%,email.ilike.%${search}%,company.ilike.%${search}%,notes.ilike.%${search}%`
       Updated: `name.ilike.%${search}%,email.ilike.%${search}%,company.ilike.%${search}%,notes.ilike.%${search}%,category.ilike.%${search}%`

    2. DASH-05 triage exit: In PUT /api/contacts/[id]/route.ts, add `ai_confidence: "manual"` to the update payload.
       After `const { id: _id, ...updateData } = parsed;`, the .update() call should include ai_confidence:
       `.update({ ...updateData, ai_confidence: "manual", updated_at: new Date().toISOString() })`
       This marks any user-edited contact as "reviewed", removing it from the triage queue.

    3. Add test cases to src/__tests__/api/contacts.test.ts:
       - Test: search query includes category in .or() clause (verify mockFrom("contacts") chain includes .or() with category)
       - Test: PUT sets ai_confidence to "manual" (verify .update() call includes ai_confidence: "manual")
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/contacts.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - `grep "category.ilike" src/app/api/contacts/route.ts` confirms category in search clause
    - `grep 'ai_confidence.*manual' src/app/api/contacts/[id]/route.ts` confirms triage exit on edit
    - New test cases pass alongside existing tests
  </done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regressions:
```bash
npx vitest run --reporter=verbose
```
All existing tests plus new risk, dashboard-stats, dashboard-analytics, and enhanced contacts tests must pass.
</verification>

<success_criteria>
1. computeContactRisk utility correctly identifies overdue, healthy, and unknown contacts
2. GET /api/dashboard/stats returns aggregated risk, triage, action items, and draft stats
3. GET /api/dashboard/analytics returns time-series draft data by month with period filtering
4. Contacts search includes category text matching
5. Editing a contact sets ai_confidence='manual' to exit triage queue
6. All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-intelligence/04-01-SUMMARY.md`
</output>

---
phase: 04-dashboard-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(dashboard)/dashboard/page.tsx
  - src/components/dashboard/stat-card.tsx
  - src/components/dashboard/risk-contacts.tsx
  - src/components/dashboard/triage-contacts.tsx
  - src/components/dashboard/pending-actions.tsx
  - src/components/dashboard/outreach-analytics.tsx
  - src/components/contacts/contact-card.tsx
  - src/__tests__/components/dashboard.test.tsx
  - src/__tests__/components/outreach-analytics.test.tsx
  - src/__tests__/components/contact-card.test.tsx
  - package.json
autonomous: true
requirements: [DASH-01, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07]

must_haves:
  truths:
    - "Dashboard page shows summary stat cards for total contacts, at-risk count, triage count, pending actions count"
    - "Dashboard page shows list of at-risk contacts sorted by most overdue"
    - "Dashboard page shows list of contacts needing triage (unreviewed AI-created)"
    - "Dashboard page shows pending action items across all contacts with contact name"
    - "Dashboard page shows outreach analytics chart with sent/dismissed/pending by month"
    - "Contact cards on contacts list page show visual risk indicator (colored left border) for overdue contacts"
    - "Contact cards show triage badge for unreviewed AI contacts"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Intelligence hub dashboard"
      min_lines: 80
    - path: "src/components/dashboard/risk-contacts.tsx"
      provides: "At-risk contacts widget"
      min_lines: 30
    - path: "src/components/dashboard/triage-contacts.tsx"
      provides: "Triage contacts widget"
      min_lines: 30
    - path: "src/components/dashboard/pending-actions.tsx"
      provides: "Pending action items widget"
      min_lines: 30
    - path: "src/components/dashboard/outreach-analytics.tsx"
      provides: "Recharts analytics chart"
      min_lines: 40
    - path: "src/components/dashboard/stat-card.tsx"
      provides: "Reusable stat display card"
      min_lines: 15
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "/api/dashboard/stats"
      via: "fetch in useEffect"
      pattern: "fetch.*api/dashboard/stats"
    - from: "src/components/dashboard/outreach-analytics.tsx"
      to: "/api/dashboard/analytics"
      via: "fetch in useEffect"
      pattern: "fetch.*api/dashboard/analytics"
    - from: "src/components/contacts/contact-card.tsx"
      to: "risk_level, needs_triage props"
      via: "conditional border color and badge rendering"
      pattern: "risk_level|needs_triage"
---

<objective>
Build the dashboard intelligence UI: transform the placeholder dashboard page into an actionable hub with at-risk contacts, triage queue, pending action items, and outreach analytics charts. Also enhance contact cards with visual risk/triage indicators.

Purpose: This is the user-facing intelligence layer that makes the product sticky -- users see at a glance which relationships need attention, what actions are pending, and how their outreach is performing.

Output: Fully functional dashboard page with 4 widgets + analytics chart, enhanced contact cards with risk/triage indicators, recharts installed for analytics visualization.
</objective>

<execution_context>
@/Users/Matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-intelligence/04-RESEARCH.md
@.planning/phases/04-dashboard-intelligence/04-01-SUMMARY.md

<interfaces>
<!-- API contracts from Plan 01 that this plan consumes -->

GET /api/dashboard/stats response:
```typescript
{
  data: {
    at_risk_contacts: Array<{
      id: string; name: string; email: string | null;
      company: string | null; category: string | null;
      days_overdue: number; risk_level: "critical" | "warning";
    }>;
    triage_contacts: Array<{
      id: string; name: string; email: string | null;
      company: string | null; category: string | null;
      ai_confidence: "high" | "medium" | "low";
    }>;
    pending_actions: Array<{
      id: string; text: string; contact_id: string;
      contacts: { name: string };
    }>;
    draft_stats: { total: number; sent: number; pending: number; dismissed: number };
    summary: {
      total_contacts: number; at_risk_count: number;
      triage_count: number; pending_actions_count: number;
    };
  }
}
```

GET /api/dashboard/analytics?period=30d response:
```typescript
{
  data: {
    by_month: Array<{
      month: string; // "2026-01"
      sent: number; dismissed: number; pending: number; total: number;
    }>;
    totals: { sent: number; dismissed: number; pending: number; total: number };
  }
}
```

From src/lib/contacts/risk.ts:
```typescript
export type RiskLevel = "critical" | "warning" | "healthy" | "unknown";
export interface ContactRisk { days_overdue: number; is_at_risk: boolean; risk_level: RiskLevel; }
export function computeContactRisk(contact: { ... }): ContactRisk;
```

Existing ContactCard props (src/components/contacts/contact-card.tsx):
```typescript
interface ContactCardProps {
  contact: {
    id: string; name: string; email?: string | null; company?: string | null;
    role?: string | null; category?: string | null;
    last_interaction_at?: string | null; status?: string | null;
    action_items?: { id: string; completed: boolean }[];
  };
}
```

Existing codebase UI patterns:
- "use client" pages with useEffect + useState for data fetching
- fetchX functions wrapped in useCallback
- toast via sonner for errors
- Loading states with Skeleton components
- Empty states with centered icon + message
- Grid: grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3
- Components in src/components/{domain}/ folders
- Tests use vi.hoisted mocking pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts and build dashboard page with widgets</name>
  <files>package.json, src/app/(dashboard)/dashboard/page.tsx, src/components/dashboard/stat-card.tsx, src/components/dashboard/risk-contacts.tsx, src/components/dashboard/triage-contacts.tsx, src/components/dashboard/pending-actions.tsx, src/__tests__/components/dashboard.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/page.tsx (current placeholder to replace)
    - src/app/(dashboard)/drafts/page.tsx (reference for "use client" page pattern with fetch-on-mount)
    - src/app/(dashboard)/contacts/page.tsx (reference for loading/empty states, grid layout)
    - src/components/contacts/contact-card.tsx (Card/Badge pattern reference)
    - .planning/phases/04-dashboard-intelligence/04-01-SUMMARY.md (API contracts from Plan 01)
  </read_first>
  <action>
    Install recharts: `npm install recharts@^3.8.0`

    Create src/components/dashboard/stat-card.tsx:
    - "use client" component
    - Props: { title: string; value: number | string; description?: string; icon: LucideIcon; variant?: "default" | "warning" | "critical" }
    - Uses shadcn Card with icon, large value display, and description text
    - variant="warning" -> amber/yellow accent, variant="critical" -> red accent, default -> neutral

    Create src/components/dashboard/risk-contacts.tsx:
    - "use client" component
    - Props: { contacts: Array<{ id, name, email, company, category, days_overdue, risk_level }> }
    - Renders a shadcn Card with CardHeader "At Risk" and CardContent as a list
    - Each item: contact name (linked to /contacts/[id]), days overdue as badge, risk_level as colored dot (critical=red, warning=amber)
    - Empty state: "No contacts at risk" with CheckCircle icon
    - Show max 5 contacts, with "View all N at-risk contacts" link if more

    Create src/components/dashboard/triage-contacts.tsx:
    - "use client" component
    - Props: { contacts: Array<{ id, name, email, company, category, ai_confidence }> }
    - Renders a shadcn Card with CardHeader "Needs Review" and CardContent as a list
    - Each item: contact name (linked to /contacts/[id]), ai_confidence as badge, category if present
    - Empty state: "All contacts reviewed" with CheckCircle icon
    - Show max 5 contacts, with "Review all N contacts" link if more

    Create src/components/dashboard/pending-actions.tsx:
    - "use client" component
    - Props: { actions: Array<{ id, text, contact_id, contacts: { name: string } }> }
    - Renders a shadcn Card with CardHeader "Pending Actions" and CardContent as a list
    - Each item: action text (truncated to 80 chars), contact name as subtle label, linked to /contacts/[contact_id]
    - Empty state: "No pending action items" with CheckCircle icon
    - Show max 8 actions, with "View all N actions" link if more

    Rewrite src/app/(dashboard)/dashboard/page.tsx:
    - "use client" with useEffect + useState + useCallback pattern
    - Fetch /api/dashboard/stats on mount
    - Loading state: 4 Skeleton stat cards + 3 Skeleton content cards
    - Error state: toast via sonner
    - Layout:
      - Top row: 4 StatCard components in grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4
        - Total Contacts (Users icon), At Risk (AlertTriangle icon, variant based on count), Needs Triage (UserCheck icon), Pending Actions (ListTodo icon)
      - Bottom row: 3-column grid (lg:grid-cols-3)
        - RiskContacts widget
        - TriageContacts widget
        - PendingActions widget
    - Import icons from lucide-react: AlertTriangle, Users, UserCheck, ListTodo, CheckCircle

    Write src/__tests__/components/dashboard.test.tsx:
    - Mock global fetch to return dashboard stats data
    - Test: renders 4 stat cards with correct values
    - Test: renders at-risk contacts list
    - Test: renders triage contacts list
    - Test: renders pending actions list
    - Test: shows loading skeletons initially
    - Test: shows empty states when arrays are empty
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/dashboard.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Dashboard page fetches /api/dashboard/stats and renders 4 stat cards + 3 widget cards
    - `grep "use client" src/app/(dashboard)/dashboard/page.tsx` confirms client component
    - `grep "api/dashboard/stats" src/app/(dashboard)/dashboard/page.tsx` confirms API integration
    - `grep "StatCard" src/app/(dashboard)/dashboard/page.tsx` confirms stat cards rendered
    - `grep "RiskContacts" src/app/(dashboard)/dashboard/page.tsx` confirms risk widget
    - `grep "TriageContacts" src/app/(dashboard)/dashboard/page.tsx` confirms triage widget
    - `grep "PendingActions" src/app/(dashboard)/dashboard/page.tsx` confirms actions widget
    - `grep "recharts" package.json` confirms recharts installed
    - All dashboard tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Outreach analytics chart component</name>
  <files>src/components/dashboard/outreach-analytics.tsx, src/__tests__/components/outreach-analytics.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/page.tsx (dashboard page created in Task 1 -- to understand where this component will be placed)
    - .planning/phases/04-dashboard-intelligence/04-RESEARCH.md (recharts code example and SSR pitfall)
  </read_first>
  <action>
    Create src/components/dashboard/outreach-analytics.tsx:
    - "use client" component (CRITICAL: recharts requires client-side rendering)
    - Self-contained: fetches /api/dashboard/analytics internally with useEffect + useState
    - Props: none (self-fetching component)
    - State: period ("7d" | "30d" | "90d" | "all"), default "30d"
    - Period selector: row of 4 small buttons (7d, 30d, 90d, All) using shadcn Button variant="ghost" or variant="outline", active one uses variant="default"
    - Chart: recharts BarChart inside ResponsiveContainer (width="100%" height={300})
      - XAxis dataKey="month", YAxis with tick count
      - 3 Bar components: sent (fill="#22c55e" green-500), dismissed (fill="#ef4444" red-500), pending (fill="#3b82f6" blue-500)
      - Tooltip component for hover details
      - Legend component below chart
    - Below chart: summary row showing totals.sent, totals.dismissed, totals.pending as inline stats
    - Wrapped in shadcn Card with CardHeader "Outreach Analytics" and period selector
    - Empty state: when by_month is empty or totals.total === 0, show "No outreach data yet. Drafts will appear here once generated." with BarChart3 icon
    - Loading state: Skeleton inside the card while fetching

    After creating the component, add it to the dashboard page:
    - Import OutreachAnalytics in dashboard/page.tsx
    - Add below the 3-widget row as a full-width card: `<OutreachAnalytics />`

    Write src/__tests__/components/outreach-analytics.test.tsx:
    - Mock global fetch to return analytics data with by_month array and totals
    - Test: renders chart container (ResponsiveContainer)
    - Test: renders period selector buttons (7d, 30d, 90d, All)
    - Test: shows empty state when no data
    - Test: displays total stats below chart
    - Note: recharts internals (SVG bars) are hard to test in jsdom; focus on container and text content
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/outreach-analytics.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - `grep "use client" src/components/dashboard/outreach-analytics.tsx` confirms client component
    - `grep "recharts" src/components/dashboard/outreach-analytics.tsx` confirms recharts import
    - `grep "ResponsiveContainer" src/components/dashboard/outreach-analytics.tsx` confirms chart wrapper
    - `grep "BarChart" src/components/dashboard/outreach-analytics.tsx` confirms bar chart usage
    - `grep "api/dashboard/analytics" src/components/dashboard/outreach-analytics.tsx` confirms API fetch
    - `grep "OutreachAnalytics" src/app/(dashboard)/dashboard/page.tsx` confirms integration
    - All analytics component tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Contact card risk and triage indicators</name>
  <files>src/components/contacts/contact-card.tsx, src/app/(dashboard)/contacts/page.tsx, src/__tests__/components/contact-card.test.tsx</files>
  <read_first>
    - src/components/contacts/contact-card.tsx (current card to enhance -- DO NOT rebuild, only add indicators)
    - src/app/(dashboard)/contacts/page.tsx (contacts page that passes data to ContactCard)
    - src/app/api/contacts/route.ts (current GET returns contacts -- verify what fields are available)
    - src/__tests__/components/contact-card.test.tsx (existing tests to extend)
    - src/lib/contacts/risk.ts (risk computation utility from Plan 01)
  </read_first>
  <action>
    Enhance ContactCard (src/components/contacts/contact-card.tsx):

    1. Extend ContactCardProps interface to add optional computed fields:
       ```
       contact: {
         // ... existing fields unchanged
         days_overdue?: number;
         risk_level?: "critical" | "warning" | "healthy" | "unknown";
         needs_triage?: boolean;
       }
       ```

    2. Add visual risk indicator:
       - If risk_level === "critical": add red left border to Card (`border-l-4 border-l-red-500`)
       - If risk_level === "warning": add amber left border (`border-l-4 border-l-amber-500`)
       - If risk_level === "healthy" or undefined: no extra border (existing styling)
       - Show days_overdue as a small badge next to last interaction: "{N}d overdue" in red/amber text

    3. Add triage indicator:
       - If needs_triage === true: show a small Badge variant="outline" with text "Needs review" in the card header area, next to the status dot
       - Use a subtle style that doesn't overwhelm the card

    4. Keep all existing rendering exactly as-is. These are purely additive changes.

    Update contacts page (src/app/(dashboard)/contacts/page.tsx):
    1. Import computeContactRisk from "@/lib/contacts/risk"
    2. Extend the Contact interface to include: outreach_frequency_days, ai_confidence, created_at (these fields already come from the API since it does `select("*")`)
    3. After fetching contacts, map through them to compute risk fields:
       ```
       const enrichedContacts = (json.data ?? []).map(c => ({
         ...c,
         ...computeContactRisk(c),
         needs_triage: c.ai_confidence !== "manual",
       }));
       ```
    4. Pass enriched contacts to ContactCard

    Extend src/__tests__/components/contact-card.test.tsx:
    - Test: shows red left border when risk_level is "critical"
    - Test: shows amber left border when risk_level is "warning"
    - Test: shows "overdue" text when days_overdue > 0
    - Test: shows "Needs review" badge when needs_triage is true
    - Test: no risk/triage indicators when fields are undefined (backward compatibility)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/contact-card.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - `grep "risk_level" src/components/contacts/contact-card.tsx` confirms risk props added
    - `grep "needs_triage" src/components/contacts/contact-card.tsx` confirms triage props added
    - `grep "border-l-red" src/components/contacts/contact-card.tsx` confirms critical indicator
    - `grep "border-l-amber" src/components/contacts/contact-card.tsx` confirms warning indicator
    - `grep "computeContactRisk" src/app/(dashboard)/contacts/page.tsx` confirms risk enrichment
    - `grep "needs_triage" src/app/(dashboard)/contacts/page.tsx` confirms triage enrichment
    - All existing contact card tests still pass
    - New risk/triage indicator tests pass
    - Existing category filter test passes (DASH-03): `npx vitest run src/__tests__/api/contacts.test.ts` confirms category filtering still works
  </done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regressions across the entire codebase:
```bash
npx vitest run --reporter=verbose
```
All tests from Phases 1-4 must pass. Then verify the build compiles:
```bash
npx next build 2>&1 | tail -20
```
</verification>

<success_criteria>
1. Dashboard page is a fully functional intelligence hub with stat cards, risk/triage/actions widgets, and analytics chart
2. Analytics chart renders outreach data by month using recharts BarChart with period selector
3. Contact cards show visual risk indicators (colored left border) and triage badge
4. Contacts page enriches cards with computed risk and triage data from the risk utility
5. All new and existing tests pass
6. Build compiles without errors (especially no recharts SSR issues)
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-intelligence/04-02-SUMMARY.md`
</output>
